@model IEnumerable<Plingy.Models.Allergies>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Allergy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Allergy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AllergiesID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AllergiesID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AllergiesID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
